const base = '/api';
const pages = `${base}/pages`;
const data = `${base}/data`;

export const homeUrlPath = `${pages}/home`;
export const commonUrlPath = `${pages}/common`;
export const nodesUrlPath = `${data}/nodes`;
export const podsUrlPath = `${data}/pods`;
